{
  "name": "lambda-watcher",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "bin": "./lib/index.js",
  "scripts": {
    "copy-adapters": "cp -r ./src/adapters ./lib/",
    "build": "esbuild src/watcher.ts --bundle --outfile=lib/index.js --platform=node --external:fsevents --external:nodemon && yarn copy-adapters"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.95",
    "@types/nodemon": "^1.19.1",
    "@types/yargs": "^17.0.10",
    "commander": "^9.2.0",
    "fastify": "^3.28.0",
    "ts-node": "^10.7.0",
    "typescript": "^4.6.3"
  },
  "dependencies": {
    "@fastify/multipart": "^6.0.0",
    "esbuild": "^0.14.38",
    "nodemon": "^2.0.15"
  }
}
